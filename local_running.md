# Install bundler
```bash
sudo apt update
```

```bash 
sudo apt -y install bundler
```

# Install local dependencies
```
sudo bundle install
```

# Launch app in local
```bash
bundle exec jekyll serve
```


